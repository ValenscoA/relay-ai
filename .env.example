DATABASE_URL=postgres://relay:relay@localhost:5432/relay
# 32-byte base64 key: openssl rand -base64 32
APP_ENCRYPTION_KEY=
# Comma-separated hostnames allowed for custom provider endpoints
ALLOWED_PROVIDER_HOSTS=api.openai.com,openrouter.ai
